version: "3.7"
services:
  db:
    image: 'postgres'
    environment:
      POSTGRES_PASSWORD: 123456

  web: 
    build: 
      context: .
    ports:
      - 8000:8000
    environment:
      - DATABASE_URL=postgres://postgres:123456@db/postgres
      - DISABLE_RECAPTCHA
    depends_on:
      - db
